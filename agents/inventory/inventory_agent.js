<CODE_BLOCK>
```javascript
// inventory_agent.js

/**
 * Monitors the inventory level for a given SKU.
 * @param {string} sku - The Stock Keeping Unit to monitor.
 */
function monitorInventory(sku) {
  console.log(`Monitoring inventory for sku: ${sku}`)
}

// Example usage:
monitorInventory("example_sku");

module.exports = { monitorInventory };
```
</CODE_BLOCK>
